import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

// eslint-disable-next-line
console.log(
	"Hi there 🖖, and thanks for checking out my code. This site was built in a weekend with React and I wrote and open-sourced the react-tag-cloud ☁️ component while I was at it. Don't forget to put on some 3D goggles 😎. Nice to meet you, IjzerenHein out."
);

ReactDOM.render(<App />, document.getElementById('root'));
